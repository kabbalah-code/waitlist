import type { ReactNode } from "react"

export const metadata = {
  title: "Dashboard | KABBALAH CODE",
  description: "Your mystical journey awaits",
}

export default function DashboardLayout({ children }: { children: ReactNode }) {
  return children
}
